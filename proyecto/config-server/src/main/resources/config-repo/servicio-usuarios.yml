biblioteca:
  mensaje-bienvenida: >-
    servicio-usuarios listo (config-server)
spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: >-
            http://localhost:9000/oauth2/jwks
